| 記録する項目     | 内容                                                                |
| ---------- | ----------------------------------------------------------------- |
| 登録方法       | MVPは本人のみが利用し、登録画面を作らずSeederで登録する                                  |
| 既存コードの確認結果 | Factoryの`name`・`email_verified_at`・`remember_token`が現在のテーブルと一致しない |
| 登録処理の方針    | Factoryを経由せず、SeederからUserモデルを使用する                                 |
| パスワードの扱い   | 実際の値は`.env`に置き、設定ファイル経由で取得する。保存時は`hashed`キャストを利用する                |
| 再実行時の方針    | 同じメールアドレスが登録済みなら追加・更新しない                                          |
| 現在の進捗      | 方針整理まで完了。`.env`のGit除外確認と実装・動作確認は未実施                               |
